# Phase 1 — Project setup (checkpoint)

Selesai di workspace ini (tanpa GitHub/Vercel remote — belum ada kredensial):

1. Next.js + TypeScript + Tailwind — `fleet-web/`
2. ESLint — default Next
3. `.env.example` — URL + anon key; service role tidak di-prefix NEXT_PUBLIC
4. Struktur folder: `app/` (eksisting), `lib/supabase/`, `lib/services/`, `types/`, `supabase/migrations/`
5. shadcn/ui: belum di-init (UI diblok sampai Phase 2–4 selesai sesuai handover)

**Tidak dilakukan (butuh akun Anda):** GitHub repo, Vercel project, Supabase project connect.

Lanjut: Phase 2 database (lihat `docs/PHASE-02-CHECKLIST.md`).
